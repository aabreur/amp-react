version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
test:
  phases:
    preTest:
      commands:
        - npm ci
    test:
      commands:
        - npx prettier --check src
        - npm run test -- --coverage --watchAll=false
  artifacts:
    baseDirectory: coverage
    files:
      - '**/*.*'